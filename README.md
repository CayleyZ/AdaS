# AdaS

This repository contains the minimal SpikeLM fine-tuning bundle in [`spikelm/`](spikelm/).

The large pretrained checkpoint is distributed as a GitHub Release asset instead of being tracked in git.
